#include "main.h"

/**
 * swap_int - swaps the value of two integers.
 * @: first integer.
 * @b: second integer.
 * @temp: third integer.
 * Return: no return.
 */
void swap_int(int *a, int *b)
{
	int temp = *a;
	*a = *c;
	*b = c;
}
